# TS-07 决策记录

## 当前状态

- 证据状态：`harness_ready`；
- 审计有效性：二次审查已补齐失败状态、切段序列、降级转换和漂移门禁；授权后复测在快速切换到第 2 段并定位 10 秒时超时；
- 候选门槛：未完成，未生成 `candidate-run.json`；
- 最终状态：`harness_ready`，不沿用旧 `conditional_pass`。

## 结果

现有高保真 Demo 的正常真实音频路径达到 TS-07 的 `<250ms` 门槛：字幕、视觉、暂停恢复和重复定位均通过，50 次定位后的字幕、画面和当前段进度一致，没有累计漂移问题。

首轮降级路径未达到门槛。5/5 运行在数学第 1 段约 10 秒处发生音频错误后，都从约 49.6%–49.7% 跳回约 0.2%–0.5%。交接误差 P95 约 9965ms，进度非单调，字幕和画面回到开头。

该首轮结果作为修复前红态保留。最小修复让共享视觉降级继承最后有效音频比例和已解码时长。修复后在数学第 1/2 段间交替执行 10 次切段定位，P95 约 123.7ms、最大约 132.9ms、漂移斜率约 2.20ms/次，10/10 当前段与字幕、画面、进度一致；5 次降级交接 P95 约 25.1ms、最大约 25.7ms，5/5 单调继续、无字幕/画面回滚且每次恰好一次真实音频到视觉时钟的状态转换。声音主讲解和真空主题冒烟检查也通过了同一交接及降级暂停/恢复边界。

修复前机器违规码：

- `fallback.handoff_error_exceeded`；
- `fallback.not_monotonic`；
- `fallback.state_rollback`。

历史复测的机器违规码为空，但该结果早于二次门禁收紧，只作为诊断证据，不支持当前通过结论。

## 人工评审

2026-08-04，评审人 `junxxxxiao` 在本地独立运行 `python3 spikes/ts-07-unified-playback-timeline/run_validation.py`，确认 `validation_pass=true`、`candidate_gate_pass=true`且 `violation_codes=[]`。评审人接受 Safari、微信 WebView、真机、低端设备、后台限流、网络缓冲和长讲解仍未验证，并确认本结论不代表完整 P0 链路通过。

可追溯审核产物：[PR #5 人工评审评论](https://github.com/junxxxxiao/primary-visual-learning/pull/5#issuecomment-5177367523)。

该评审早于二次审查新增的跨段音频哈希、真实时钟转换事件与非递增漂移门禁，只作为历史记录保留；新候选运行后必须重新人工确认。

## 原因与修复

修复前 `startVisualFallback` 使用独立的 9 秒固定视觉时钟，并从 `visualFallbackRatio = 0` 开始。媒体错误处理没有把真实音频位置映射到降级比例。修复后共享播放器在真实音频健康时记录最后有效比例和时长，媒体错误时交给现有降级时钟；音频从未提供有效元数据时才保留 9 秒默认值。

## 影响

- PRD：同步明确播放中失败必须继承当前进度和已解码时长；
- 高保真与原型：共享播放器交互外观不变，只修复声音、数学和真空主题共用的降级交接；
- 架构：仅记录当前媒体位置与时长快照，未将 Demo 抽取成生产运行时，也未引入预设专用计时器；
- 数据与隐私：无变化；未使用儿童数据、外部服务、凭据或生产日志；
- TS-04C：不受本轮结果提升，当前失败且未合入基线的限制保持不变。

## 已验证与未验证

已验证：固定数学片段在本机 Chrome 中的正常 cue 同步、暂停恢复、重复定位，以及首轮修复后媒体错误的连续交接；这些是历史诊断证据，不是当前候选通过结论。

未验证：高频连续切段定位、Safari、微信 WebView、真机、低端设备、后台限流、网络缓冲、长讲解、生产会话隔离和生成视觉场景；ADR-0005 中的自动连播切段、主题切换及返回原主题、每段起始画面精确静置 1 秒、减少动态效果、前后台恢复、旧会话延迟事件、问题切换、受限命令和刷新后的兼容快照恢复也未进入本轮分母。

## 下一决策

TS-07 以 `harness_ready` 合并当前测试框架与失败记录，不抽取生产代码。正常 Demo 体验未复现问题，高频连续切段压力场景暂缓；后续进入生产播放器或真实用户证据显示该操作重要时再验证。完整 P0 链路仍由尚未通过的 TS-04C 阻断。
