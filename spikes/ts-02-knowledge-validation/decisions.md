# TS-02 决策记录

- 日期：2026-07-28
- 结论：**有条件通过（受控章节已核对，数学来源缺口未关闭）**
- 基线：`origin/main` at `b1632717167bc620154a82aa31cb83eb1f4fd094`
- 负责人：Codex 技术切片执行

## 决策

保留“已核验知识原子、临时知识包、未核验生成”三条内容路线，并将阻断、静态/媒体回退和固定安全响应作为不同状态处理。

年级和教材是可选适配信息。缺少学段时继续讲解并记录 `stage_fit=unknown`，不得猜测；明确超前时优先寻找适龄方法，没有适龄替代时仍可正常讲解并记录 `stage_fit=advanced`，讲解中不显示超纲提示。

知识原子未命中但找到可靠来源时，可建立临时知识包。知识原子和可靠来源都未命中时，仍正常生成讲解，在开头一次性展示固定参考提示，不要求确认；该内容标记 `unverified_generated`，不能成为已核验缓存、正式迁移学习结果或长期画像。

未声明的跨学段串源、严重核验冲突、OCR 污染、提示注入、可验证计算失败以及安全问题不能用免责声明绕过，继续阻断或使用固定响应。

## 核心证据

- 固定输入：13 个；预期发布 6 个、未核验生成 3 个、阻断 4 个；
- Oracle 路由准确率：`13/13 = 100%`；
- 来源页码可追溯率：`10/10 = 100%`；
- 已提供学段字段完整率：`30/30 = 100%`；
- 缺学段继续且不猜测：`1/1`；显式超前继续讲解：`1/1`；
- 未声明跨学段误发布：`0/1`；检查冲突误发布：`0/1`；
- 未核验路由：`3/3`；固定提示覆盖：`3/3`；正式结果写入：`0/3`；
- 阻断路由：`4/4`；Schema 合法率：`109/109 = 100%`；
- 所有 `13/13` 产物均标记 `review_method=ai_only`；
- 发布产物导出为带来源文件哈希、逐条证据摘录和内容包哈希的 `source_scope=synthetic_fixture` 密封包；下游篡改或来源变化会被调用前门禁阻断；
- 受控科学来源 SHA-256：`2dceadd42bfaa05cc753c4d30cdee8fda03351fa41e830f920632aa723163774`，只检查 PDF 页 47-60；
- 受控数学来源 SHA-256：`925db6f76d1881b46f51dace670226e2d1edede9b51d161e1828284d444ec8aa`，只检查 PDF 页 101-130。

## 受控教材结论

科学第五单元支持声音强弱与振幅、声音高低与振动快慢。数学第十四章支持完全平方公式，但指定页段没有“配方求最值”“最大面积”或“围栏”证据。

因此，之前的完整数学 Demo 并不是通过运行时检索找到了可靠来源。它是人工编排的固定演示内容；如果同样的问题进入未来动态管线，完全平方公式部分可由当前教材支持，但包含配方最值和围栏迁移的完整内容包应标记 `unverified_generated`，正常讲解并展示一次参考提示，不得形成正式学习结果。固定 Demo 本身不新增提示页或设置页。

## 保留的失败与路由样例

| 样例 | 关键原因码 | 裁决 |
|---|---|---|
| `missing_stage_context` | 无；不得猜测 | `publish`，`stage_fit=unknown` |
| `declared_advanced_path` | 无；显式超前 | `publish`，`stage_fit=advanced` |
| `absent_evidence` | `CRITICAL_CLAIM_WITHOUT_EVIDENCE` | `unverified_generate` |
| `no_atom_no_authoritative_source` | `NO_RELIABLE_SOURCE` | `unverified_generate` |
| `missing_page` | `PAGE_MISSING`, `EVIDENCE_PAGE_MISSING` | `unverified_generate` |
| `cross_stage_source_mixing` | `CROSS_STAGE_SOURCE_MIXING` | `block` |
| `verification_conflict` | `INDEPENDENT_CHECK_CONFLICT` | `block` |
| `ocr_error` | `OCR_LOW_CONFIDENCE`, `EVIDENCE_QUOTE_NOT_FOUND` | `block` |
| `prompt_injection` | `PROMPT_INJECTION_DETECTED` | `block` |

## 对 PRD 的影响

已同步 PRD v5：年级/教材改为可选适配信息；缺失或超前不阻断；新增三条内容路线、固定未核验提示和正式结果隔离；安全、冲突和计算失败仍不可绕过。公开儿童内测门槛增加未核验状态、提示、结果排除和纠错反馈验证。

## 对高保真设计和公开 Demo 的影响

产品原型规范已记录首次设置的可选说明和未来动态提示状态。固定公开 Demo 继续展示人工编排内容，不新增设置页、研究状态页或未核验提示，也不修改 `prototype/sound-demo.html`。这样避免把尚未实现的动态检索状态伪装成 Demo 已具备能力。

## 对架构的影响

候选架构保留版本化 `StageProfile`、独立 `StageFit`、不可变来源哈希与页码、五个分离检查结果、结构化发布裁决、`verification_status`、固定用户提示和机器可读原因码。`unverified_generated`、`blocked`、静态/媒体回退和固定安全响应必须是不同状态。

本结论不锁定 Python、JSON Schema 库、模型供应商、OCR、数据库、队列或部署框架，`src/` 不得直接迁入生产模块。

## 对数据和隐私的影响

两份本地教材 PDF 未复制、修改或提交，仓库不保存教材正文，只记录文件哈希、限定章节页码、字符数、文本哈希和访问规则。没有使用儿童数据、外部网络、密钥或生产日志。

未核验生成只保存必要的模型版本、输入摘要、内容哈希、检查结果和纠错反馈，不产生正式学习结果或长期画像。教材进一步使用的授权范围、保存期限、访问控制和衍生内容权利仍需单独确认。

## 已验证范围

- 小学声音、初中完全平方公式和单个双来源临时知识包夹具；
- 缺学段、显式超前、无证据、无可靠来源、缺页、跨学段串源、冲突、OCR 污染和提示注入路由；
- 科学 PDF 页 47-60 与数学 PDF 页 101-130 的哈希、文本层完整性和关键证据存在性；
- 版本化输入/输出、独立步骤、三条内容路线、提示覆盖和结果隔离。

## 仍未验证范围

- 教材版次、印次、授权用途、保存期限和受控访问责任人；
- 真实模型的独立性、随机性、重复运行一致性、供应商差异与注入鲁棒性；
- 真实搜索的来源等级判断、版权授权、来源冲突消解和临时包时效；
- PDF 公式/图表语义提取、扫描 OCR 与缺页识别泛化；
- 教师或学科专家审核、事实准确率和教学适配效果；
- 延迟、吞吐、失败重试、可观测性、费用、儿童可用性和生产稳定性；
- 三个固定知识簇以外的学科、年级、教材和知识点。

## 下一步

1. 用冻结的真实模型配置运行同一 12 例协议，增加重复次数、独立核验、延迟和成本分母；
2. 增加经过授权且直接覆盖配方、二次式最值和围栏建模的可靠来源，再判断数学包能否从未核验升级为临时知识包；
3. 确认两份 PDF 的版次、授权用途、保存期限和受控访问责任人；
4. 在公开儿童内测前完成未核验提示的可理解性、结果隔离、固定安全响应和人工内容评审。

## 2026-07-29 真实模型首轮补充

经用户明确授权，只向第三方聚合服务 PackyAPI 发送仓库内合成夹具，使用请求模型 `deepseek-v4-flash` 完成一轮 12 个样例、60 个隔离核验步骤。没有发送教材 PDF、教材提取正文、儿童数据或身份信息。聚合服务在 59 个成功响应中返回的实际模型标识均为 `deepseek-v4-flash-202605`；本切片无法独立证明该标识与 DeepSeek 官方模型的映射关系。

首轮结果为失败，不改变前述确定性管线的“有条件通过”，但证明当前真实模型配置尚不能进入 TS-02 候选发布链：

- 最终路由准确率：`7/12 = 58.3%`；
- 步骤 Schema 合法率：`53/60 = 88.3%`；
- 步骤 pass/fail 准确率：`53/60 = 88.3%`；
- 原因码完全一致率：`49/60 = 81.7%`；
- API 响应：`59/60`，其中一次超过 120 秒；
- Token：输入 `50,129`、输出及推理 `40,537`、合计 `90,666`；
- PackyAPI 未返回可用于本地确认的实际费用，不能套用 DeepSeek 官方单价。

五个最终路由失败样例为 `middle_perfect_square_valid`、`temporary_vacuum_valid`、`declared_advanced_path`、`absent_evidence` 和 `missing_page`。主要原因包括思考内容占满 `max_tokens=1200` 后没有最终 JSON、JSON 截断、缺少 `schema_version`、显式超前路径被误判为跨学段串源，以及一次网关超时。`ocr_error` 虽最终正确阻断，但额外错误地报告 `BOUNDARIES_MISSING`，说明不能只看最终路由而忽略逐步骤原因。

本轮不自动重试，也不启动第二轮。若继续，应把“关闭思考模式”与“提高最大输出 Token”作为两个分离配置进行单变量预检，先只重跑格式失败步骤；只有 Schema 门槛恢复后，才值得重新运行完整 12 例。任何重跑都需要用户再次确认。

用户选择“关闭思考模式”后，只对 7 个 Schema/超时失败步骤进行了定向预检，没有重跑其余 53 个步骤。预检结果为：Schema `7/7`、pass/fail `7/7`、原因码 `7/7`，合计 `7,084` Token，无超时。实际响应模型仍为 `deepseek-v4-flash-202605`。

若把这 7 个定向结果仅用于诊断性替换，首轮路由可由 `7/12` 改善到 `11/12`，Schema 可由 `53/60` 改善到 `60/60`；但这不是同一冻结配置下的完整一轮，不能记录为正式复测分数。仍未解决的问题是：显式超前路径被误判为跨学段/教材版本混用；无可靠来源样例漏报 `NO_RELIABLE_SOURCE`；OCR 样例漏报 `OCR_OVERRIDE_UNTRUSTED` 并多报 `BOUNDARIES_MISSING`。因此下一步如获授权，应使用关闭思考的配置完整跑一轮，而不是只重跑失败步骤或继续使用原思考配置。

### 请求关闭思考的完整复测

用户再次确认后，以 `thinking.type=disabled` 请求参数完整运行 12 例、60 步。正式结果仍为失败：

- 最终路由准确率：`10/12 = 83.3%`；
- 步骤 Schema 合法率：`55/60 = 91.7%`；
- 步骤 pass/fail 准确率：`57/60 = 95.0%`；
- 原因码完全一致率：`52/60 = 86.7%`；
- API 响应：`60/60`，无传输失败；平均延迟 `8,476 ms`；
- Token：输入 `3,395`、输出及推理 `44,312`、合计 `47,707`；
- 失败路由：显式超前路径被阻断；无知识原子且无可靠来源样例因截断 JSON 被阻断，而不是进入未核验生成。

虽然请求关闭思考，但 `60/60` 响应仍返回非空 `reasoning_content`，`5/60` 以 `finish_reason=length` 结束。因此 PackyAPI 或其上游没有实际执行关闭思考请求，这轮不能作为真正的无思考模型证据。第三方聚合平台仍未提供可由本地核实的实际费用，模型映射、请求参数支持和计费均需以其平台资料为准。

本轮进一步支持调整候选架构：来源存在性、文件哈希、页码、OCR 标记、提示注入、StageProfile、显式超前声明和跨学段来源规则均应优先由本地确定性代码执行；模型只参与证据是否支持语义结论、教学表达和迁移任务等需要语义判断的步骤。即使提高输出上限可以减少截断，也不能解决显式超前规则误判，因此在确认聚合服务参数支持并调整职责边界前，不应继续机械重复完整轮次。

### 混合职责最小验证

按上述职责边界新增并运行一次混合验证：7 个负向或未核验样例完全由本地门禁路由，5 个本地发布候选各调用证据语义、教学语义和迁移语义三个模型步骤，共 15 次请求。结果为：

- 本地确定性路由：`12/12`；
- 模型语义 Schema：`14/15`；
- 正向候选语义步骤通过：`9/15`；
- 最终路由：`8/12`；
- Token：输入 `4,980`、输出及推理 `12,933`、合计 `17,913`；
- 平均延迟：`8,622 ms`；模型响应仍全部包含 `reasoning_content`。

四个最终失败候选是 `primary_sound_valid`、`middle_perfect_square_valid`、`missing_stage_context` 和 `declared_advanced_path`。其中数学证据步骤存在一次 evidence refs 类型错误；其余主要是模型判定教学表达或迁移任务不足。复核夹具后发现，部分拒绝具有合理依据：`missing_stage_context` 的迁移提示只有“Compare a tuning fork.”，没有显式要求结论或理由；`declared_advanced_path` 的“解释另一个实数平方为何非负”与原目标语义接近；声音样例的表达和迁移文本也不足以形成稳定的儿童教学质量 oracle。

因此，混合架构的本地硬门禁方向得到支持，但当前 TS-02 结构夹具不能继续承担教学语义准确率验证。下一次语义模型验证前必须另行固定完整候选讲解、明确迁移题、人工评分 rubric 和学科 oracle；该工作属于 TS-03/教学正确性验证准备，不应通过修改现有布尔 oracle 或提示词把本轮调成通过。本轮结论保持失败，不再自动调用模型。

### 调整后的审核职责与 V4 Pro 聚焦协议

经产品规则复核，缺少年级、明确超前、无知识原子或无权威来源时是否继续讲解，均由本地确定性状态直接决定，不再交给模型复审。模型只提供“证据是否完整支持候选结论、证据之间是否冲突”的辅助判断；模型不确定、超时、格式失败或语义拒绝时，普通内容降为 `unverified_generate`，不能阻断讲解，也不能写入正式学习结果。

因此新增 8 个完全合成的证据语义夹具，分别覆盖明确支持、部分支持、直接冲突、无关引用、双来源一致、双来源冲突、无证据和证据内提示注入。V4 Pro 一次聚焦运行的候选门槛固定为：Schema `8/8`、支持状态 `8/8`、原因码完全一致 `8/8`，且 6 个预期拒绝样例中的危险误放行为 `0/6`。达到门槛最多证明该冻结配置可作为辅助审核候选；单轮结果不能证明重复稳定性，也不能授予模型最终路由权。

用户授权后，PackyAPI 请求模型 `deepseek-v4-pro` 完成一次 8 例聚焦运行：

- Schema：`8/8`；
- 支持状态：`8/8`；
- 原因码完全一致：`8/8`；
- 6 个预期拒绝样例中的危险误放行：`0/6`；
- Token：输入 `2,058`、输出及推理 `3,429`、合计 `5,487`；
- 延迟：最小 `3,606 ms`、平均 `6,804 ms`、最大 `11,287 ms`；
- 8 个响应均以 `finish_reason=stop` 完成，且均包含 `reasoning_content`；
- 聚合服务响应 model 为 `deepseek-v4-pro`，但映射与实际费用无法在本地独立证明。

本轮判定 `conditional_pass`，只授予“辅助证据审核候选”资格。它没有推翻 V4 Flash 的历史失败，也没有验证模型可决定最终路由；模型审核失败时仍进入未核验生成，而不是阻断普通讲解。

本轮对五类决策面的影响如下：

- PRD：无需改变既定 C 端规则；普通问题继续讲解，模型审核失败只改变核验等级，不改变可访问性。
- 高保真设计：无需新增页面；动态产品只需保留一次自然语言参考提示，固定公开 Demo 不修改。
- 架构：确定性门禁继续拥有最终路由权；V4 Pro 可进入后续辅助审核候选池，但不得单独授予“已核验”身份。
- 数据：模型输入继续限定为最小必要的候选结论和证据片段；本轮未发送教材正文、PDF 或儿童数据。
- 隐私：本轮未扩大数据范围；生产前仍需确认 PackyAPI 的留存、删除、日志访问、子处理方和合同边界。

仍未验证：同一 8 例的重复一致性、更多近义/隐含/多跳证据、真实教材片段、跨模型对照、真实费用和人工学科复核。不得从本轮 8/8 外推为小学到初中所有学科的审核准确率。
