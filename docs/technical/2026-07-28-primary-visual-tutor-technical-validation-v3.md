# 中小学互动视觉家教技术验证 v3

> 版本：v3.2
> 日期：2026-07-29
> 状态：验证方案已确认，已增加 TS-04C、统一分段计时契约并将 TS-07 纳入 P0
> 对应 PRD：[PRD v5](../product/2026-07-28-primary-visual-tutor-prd-v5.md)
> 替代：[技术验证 v2](2026-07-22-primary-visual-tutor-technical-validation-v2.md)

## 0. 变更摘要

v3.2 保留原 TS 编号和双学段验证边界，并增加真实生成视觉体验与统一计时门槛：

- 长期定位扩展为小学至初中数学与科学，但不把两个固定夹具外推为全年级覆盖；
- 公共契约显式携带学段、年级、教材版本和前置知识；
- TS-02、TS-03、TS-04B、TS-04C 使用小学科学与初中数学双夹具；
- TS-03 增加同一核心概念的受控双学段计划，隔离学段变量；
- TS-04B 先用确定性正负夹具证明教学与布局门禁有效；TS-04C 再消费 TS-03 的完整讲解计划，用冻结真实模型验证实际生成效果、一次修复、人工评审、耗时和成本；
- TS-07 移入 P0 并置于 TS-04C 与 TS-05 之间，先建立真实语音、字幕、动画和进度的统一时间轴，再验证拖动、追问与现场恢复；
- 所有切片使用 `StageTiming` v1.0 记录同一 trace 下的里程碑、局部 span、重试、缓存、Token 和成本；用户思考与确认时间不计入系统生成延迟；
- 学段和教材上下文允许缺失；缺失时不得自行猜测，记录为 `unknown` 并正常讲解；明确超前内容记录为 `advanced`，不作为阻断条件；
- 无知识原子且无可靠来源时进入带 C 端提示的 `unverified_generated` 路径，不得伪装为已核验内容或形成正式学习结果。

v2 已确认并由 v3 继续沿用的关键假设：

- 首个动态载体从“小程序或 H5 待定”改为“H5 核心体验 + 微信入口”；
- TS-04 从纯受控组件扩展为生成视觉代码、隔离沙箱和教学正确性契约；
- 教材 PDF、多阶段 AI 核验和临时知识包进入知识验证；
- 统一时间预算为 8 秒首个讲解、30 秒全新场景目标、约 45 秒硬降级；
- 增加固定安全响应、内容修订、熔断、30/90 天数据生命周期；
- 区分学习结果、完成方式、家庭协助和探索性证据。

本文件不是生产架构或开发排期。每个切片只回答一个可证伪问题，并记录适用范围、失败样例和对 PRD、设计、架构、数据及隐私的影响。

## 1. 公共夹具

### 1.1 小学科学：声音

- 主问题：更用力拨同一根弦，音调会更高吗？
- 词义校准：音调与响度；
- 双视图：琴弦慢放与振动记录图；
- 原场景追问：可以慢一点看吗？
- 关联追问：如果没有空气呢？
- 首次迁移：同一音叉；
- 提示后迁移：同一木琴音条。

### 1.2 初中数学：完全平方公式

- 固定题图中的完全平方配方、两道代数最值题和一道靠墙围花圃题；
- 迁移任务使用 120 米围栏，分别验证尺寸、最大面积和配方原因；
- 同题提示后答对只记录完成本题，不作为有效提示后迁移。

### 1.3 受控双学段配对夹具

使用“同一根弦在其他条件不变时，更用力主要改变振幅而不直接改变频率”这一核心关系，分别声明小学与初中学习者画像。核心事实保持一致，只改变允许使用的前置知识、术语、公式密度、视觉信息量和迁移难度，用于隔离学段变量。小学版本不得依赖尚未声明的代数或波动公式；初中版本不得仅复制小学文案并替换年级标签。

### 1.4 负向夹具

- 无可靠来源的问题；
- 未填写年级和教材的问题；
- 明确声明的超前学习路径；
- 来源冲突；
- 危险实验、医疗、隐私、不适龄和紧急安全问题；
- 诱导访问网络、存储、主 DOM、相机或麦克风的生成代码；
- 视觉表达与文字结论相反；
- 超时、崩溃、重复事件和乱序事件。

## 2. 公共契约

沿用 v1 的 QuestionEnvelope、DiagnosisEnvelope、LessonPlan、Snapshot 和 Event，并增加可选 `StageProfile` 与必填 `StageFit`。`QuestionEnvelope`、`KnowledgeAtom`、`LessonPlan`、`VisualArtifact` 和 `LearningResult` 必须保留已知的学段上下文；未知时显式记录，不保存不必要的儿童身份信息。

### 2.1 StageProfile

```json
{
  "context_status": "provided|partial|unknown",
  "school_stage": "primary|middle|null",
  "grade": "1-9|null",
  "curriculum": "国家课程标准版本|null",
  "textbook_edition": "教材版本|null",
  "prerequisite_refs": ["science.sound.vibration.basic"],
  "profile_version": "1.1"
}
```

首次设置中的年级和教材均可跳过。缺少学段、年级或教材上下文时不得从题目难度、用词或孩子身份推断，系统继续讲解并记录 `context_status=unknown|partial`。

### 2.2 StageFit

```json
{
  "status": "within_stage|advanced|ambiguous|unknown",
  "basis": "curriculum_source|textbook_source|learner_confirmation|none",
  "source_refs": [],
  "missing_prerequisite_refs": [],
  "fit_version": "1.0"
}
```

`advanced` 和 `unknown` 都允许正常讲解，C 端不显示超纲提示。意外串用其他学段来源仍失败；只有显式声明来源学段、前置知识和 `StageFit` 的超前路径才可通过。

### 2.3 KnowledgeAtom

```json
{
  "knowledge_node_id": "science.sound.force_amplitude_pitch",
  "stage_profile_ref": "stage_profile_001",
  "source_refs": [{"document_hash":"sha256:...","edition":"...","page":12}],
  "claims": [],
  "boundaries": [],
  "prerequisites": [],
  "misconceptions": [],
  "review_method": "ai_only",
  "review_steps": [],
  "status": "candidate|ai_verified|disabled",
  "content_version": "2026-07-28.1"
}
```

### 2.4 VisualArtifact

```json
{
  "artifact_id": "visual_001",
  "knowledge_node_id": "science.sound.force_amplitude_pitch",
  "stage_profile_ref": "stage_profile_001",
  "code_hash": "sha256:...",
  "sandbox_policy_version": "1.0",
  "scene_contract_version": "1.0",
  "correctness_test_version": "1.0",
  "source_version": "2026-07-28.1",
  "status": "testing|validated|disabled",
  "expires_at": null
}
```

### 2.5 ContentPackage 与 LearningResult

内容包增加 `verification_status=verified_atom|temporary_verified|unverified_generated|blocked`、`stage_fit` 和 `user_notice`。`unverified_generated` 必须显示一次固定提示，并禁止写入正式 `outcome`。

学习结果分别保存 `stage_profile_ref`（可空）、`stage_fit`、`outcome`、`completion_method`、`system_assistance`、`family_assistance`、`evidence_status` 和具体迁移维度。家庭协助缺失为 `unknown`，不得推断。未核验生成只保存探索记录和反馈，不生成“自己弄懂了”等结果。

### 2.6 StageTiming 与统一统计口径

所有切片使用 `spikes/shared/schemas/stage-timing.schema.json` 的 `StageTiming` v1.0。里程碑只标记事件发生时间，span 才表示一段耗时。`question_confirmed` 是孩子点击确认后的用户感知等待零时刻，不是一段系统耗时；`input_result_visible → question_confirmed` 的阅读、修改和决定时间标记为 `user_action_excluded`，不得进入生成性能分母。

公共里程碑名称为：

```text
input_result_visible
question_confirmed
first_meaningful_content
lesson_plan_completed
visual_generation_completed
sandbox_check_completed
visual_validation_completed
interactive_ready
fallback_ready
```

切片按职责记录 `input.asr|input.ocr`、`knowledge.local_gate|knowledge.model_audit`、`lesson.plan|lesson.transfer`、`visual.generate|visual.repair`、`sandbox.create|sandbox.worker_boot|sandbox.execute|sandbox.terminate|sandbox.destroy`、`visual.validate|cache.read|cache.invalidate`、`timeline.prepare|timeline.seek|timeline.resume`、`snapshot.save|snapshot.restore` 等 span。字段至少包含单调时钟起止、`duration_ms`、结果、重试序号、缓存状态、供应方/模型、计费量、费用和机器可读错误码。

统计必须：

- 按成功、失败、超时和降级分别报告样本数、P50/P80/P95、最大值和失败率；平均值只作补充，不能单独作为门槛；
- 冷启动、热启动、缓存命中/未命中、首次生成/修复、手机/平板、小学/初中分别报告，不得混合后用总体平均掩盖长尾；
- 所有并行 span 使用同一单调时钟，端到端耗时直接由公共里程碑相减，不能把并行局部耗时简单相加；
- 后台预生成标记为 `background_speculative`，同时记录孩子确认后的实际等待与取消/修改造成的浪费成本；
- `question_confirmed → first_meaningful_content`、`question_confirmed → interactive_ready` 和 `question_confirmed → fallback_ready` 只由 TS-06 做最终端到端判定，其他切片只判定自身局部门槛。

## 3. 切片定义

## TS-00 H5 核心运行时与微信入口

### 问题

H5 能否稳定完成录音、播放、触摸互动、SVG/Canvas/沙箱渲染、进度拖动、前后台恢复；微信能否可靠提供入口和必要权限？

### 最小实现

- H5 壳和最小微信入口壳；
- 20 秒音频、琴弦触摸场景、底部追问浮层；
- 前后台切换、网络中断和设备旋转；
- iOS/Android 微信、Safari/Chrome、iPad 和中端安卓平板。

### 候选通过条件

- 状态可预测，权限拒绝有回退；
- 音频暂停和恢复误差 P95 小于 250ms；
- 交互帧率 P95 不低于 30FPS；
- 旋转和前后台不丢失讲解现场；
- 明确记录纯小程序限制和 H5/WebView 边界。
- 记录媒体准备、首帧、暂停、恢复、触摸到可见反馈和前后台重建 span；按设备分别报告 P50/P80/P95、最大值和失败率。

## TS-01 输入、OCR、儿童语音与隐私预处理

沿用 v1 的识别准确率与隐私指标，增加主动授权的两种媒体生命周期：保留最多 30 天或处理后删除。原始媒体不得进入普通日志、内容数据库或 Git。

局部耗时分别记录 `input.asr`（停止说话到转写可见）、`input.ocr`（拍照完成到 OCR 可见）、本地隐私预处理和上传；按输入类型、设备、网络和成功/失败报告 P50/P80/P95、最大值与超时率。`input_result_visible → question_confirmed` 属于用户阅读、修改与决定时间，只作可用性观察，不计入系统处理性能。

## TS-02 教材解析、AI 核验与临时知识包

### 问题

系统能否从指定教材形成可追溯知识原子，将可靠外部来源组织为临时知识包，并在学段上下文可选的情况下，把无来源内容准确隔离到带提示的未核验生成路径，同时阻断冲突、误导和危险内容？

### 最小实现

- 小学科学声音与初中数学完全平方公式两个教材知识簇；
- 解析器、来源核验器、边界检查器、教学检查器、迁移检查器和发布裁决器；
- 来源存在性、哈希、页码、OCR 标记、提示注入、StageProfile、显式超前声明和跨学段来源规则由确定性代码执行；模型只承担证据语义、教学表达和迁移语义等不能由字段直接判定的检查；
- 来源冲突、缺页、扫描错误和诱导提示夹具；
- 学段、年级或教材版本缺失，以及跨学段来源串用夹具；
- 显式超前学习夹具；
- 运行时临时知识包使用同一来源层级；
- 无知识原子且无可靠来源时的未核验生成提示、状态和结果隔离。

### 指标与候选门槛

- 来源页码可追溯率 100%；
- 已填写 `StageProfile` 的字段完整率 100%；缺少学段上下文时自行猜测为 0；
- 缺少学段上下文和显式超前路径正常进入讲解的成功率 100%；
- 未声明的小学与初中来源串用为 0；
- 无来源关键结论被标记为已核验或临时已核验为 0；
- 无来源路径进入 `unverified_generated` 且固定 C 端提示覆盖率 100%；
- 未核验生成写入正式迁移学习结果为 0；
- 检查步骤冲突时错误发布为 0；
- Schema 合法率不低于 99%；
- 所有产物明确 `review_method=ai_only`；
- 阻断、未核验生成和视觉/媒体回退分别返回机器可读原因，不用一个“硬降级”状态混合表达。
- 本地确定性门禁与模型语义步骤分别统计，教学语义不得用结构布尔字段充当人工 oracle；进入真实模型比较前必须固定完整讲解文本、迁移题和人工评分 rubric。
- 模型语义步骤只影响 `verified|unverified` 等核验等级，不拥有缺学段、显式超前或无可靠来源问题的讲解阻断权；模型超时、格式失败或不确定时进入 `unverified_generated`。
- 辅助证据审核候选预检固定覆盖明确支持、部分支持、冲突、无关引用、双来源一致/冲突、无证据和证据内提示注入；单轮门槛为 Schema `8/8`、状态 `8/8`、原因码 `8/8`、危险误放行 `0/6`。
- 本地确定性门禁、模型证据审核和最终路由分别记录 span；按缓存、供应方、模型、结果和重试报告 P50/P80/P95、最大值、超时率、Token 与成本，不以单轮平均延迟作为稳定性结论。

2026-07-29 使用 PackyAPI 请求 `deepseek-v4-pro` 完成一次上述 8 例合成预检，达到全部候选门槛，使用 `5,487 Token`，平均延迟 `6,804 ms`，判定为辅助审核候选的 `conditional_pass`。该结果不证明重复稳定性、真实教材泛化、生产资格或聚合服务模型映射，也不改变本地确定性门禁拥有最终路由权的架构约束。

## TS-03 渐进讲解与迁移规划

### 问题

AI 核验知识能否在保留学段上下文的前提下转换为渐进讲解、视觉契约、静态降级和有效迁移任务？

### 最小实现

- 输出 2–4 个子片段；
- 分别运行小学科学声音、初中数学完全平方公式，以及同一声音核心关系的小学/初中受控配对夹具；
- 学段规则显式声明允许的前置知识、术语、公式密度、视觉信息量和迁移难度；
- 每段包含讲解目标、时间线 cue、渐进图解、视觉场景契约和降级；
- 首次迁移包含结论与因果验证；
- 失败后生成不同对象的新迁移情境；
- 关联主题不强制迁移。

### 候选门槛

- 计划 Schema 合法率不低于 99%；
- 关键顺序通过率不低于 90%；
- 学段上下文完整率 100%，使用未声明前置知识、跨学段来源或仅替换年级标签的计划为 0；
- 要求儿童先预测或等待作答的计划、换皮迁移和无降级片段为 0；
- 未经来源支持的关键事实为 0。
- 首个可用子片段、完整讲解计划、迁移题和静态降级计划分别记录 span；按学段、模型、成功/失败与重试报告 P50/P80/P95、最大值、Token 和成本。

### 当前结果（2026-07-31）

TS-03 使用 DeepSeek 官方 API `deepseek-v4-pro` 完成 `official-full-round-5`：4/4 请求返回、Schema 4/4；冻结输出在修正“前置知识可支持基础术语”的门禁误报后合同 4/4、双学段配对通过。人工学科与产品审核 4/4 通过，允许 TS-03 标记为 `pass`，无需新增模型轮次。完整计划延迟 P80 为 21,132ms，共记录 21,206 Token；真实账单成本仍未独立取得。

人工审核同时记录“讲解可能太简单、太简洁、内容较少”。数学案例覆盖了建模、配方和最大值理由，三个声音案例主要覆盖结论与对比，机制展开相对有限。该观察不推翻固定 4 例的审核通过结果，但本切片不证明讲解深度足以支持真实儿童理解。后续视觉审核与家庭测试应检查结论、原因/机制、可观察证据、条件边界和迁移是否形成完整推理链，不设置脱离教学目标的机械字数门槛。

## TS-04A 生成视觉代码沙箱

### 问题

任意生成视觉代码能否在不访问主应用和儿童数据的条件下安全运行、被限制并随时销毁？

### 必测攻击面

- 网络、Cookie、本地存储、主 DOM、摄像头、麦克风、剪贴板；
- 无限循环、内存增长、Worker/iframe 逃逸；
- 恶意 `postMessage`、事件洪泛和资源加载；
- 重复创建、崩溃和销毁后的残留状态。

### 候选门槛

- 禁止能力成功访问为 0；
- 超限代码可在预算内终止；
- 沙箱崩溃不影响主讲解和保存状态；
- 输入仅使用脱敏 Schema，输出仅限白名单事件；
- 每次执行可追溯到代码哈希和策略版本。
- 分别记录 iframe 创建、Worker 启动、代码执行、超限终止和销毁 span；正常执行与预期超时攻击不得混入同一个百分位分母。

## TS-04B 教学正确性、视觉场景布局契约与视觉缓存

### 问题

生成场景能否在展示前验证关键教学与布局不变量，并安全复用已验证产物？

### 最小实现

- 小学科学声音与初中数学完全平方公式场景契约；
- 按 [ADR-0003](../adr/0003-visual-scene-layout-contract.md) 实现单一布局验证接缝，固定原型、生成动画和静态降级图解使用同一接口；
- 输入声明手机/平板布局、画布安全内边界、场景根节点、关键状态、完整运动包络和最小可读尺寸；
- 参数组合、时间线 cue、文字/语音/视觉一致性检查；
- 对起始、关键过程、最终、暂停、恢复、交互后、减少动态效果和视口变化状态执行边界框、运动包络与 scroll/client 隐藏裁切检查；
- 正确、相反、模糊、超时、元素越界、隐藏裁切、仅中间轨迹越界和降级越界场景夹具；
- 缓存键包含学习目标、知识版本、教学契约版本、布局契约版本、代码哈希和测试版本；
- 来源或契约变更触发失效。

### 候选门槛

- 已知相反关系场景拦截率 100%；
- 已知布局违规场景拦截率 100%，每个视口和状态输出可定位违规的机器可读 `pass/fail`；
- 两个学段夹具均须分别通过手机与平板矩阵；通过只证明同一契约可承载这两个固定内容形态；
- 未通过场景展示给儿童为 0；
- 未通过场景进入有效缓存为 0；控制台警告、`overflow: hidden`、`clip` 或遮罩均不能使失败场景通过；
- 缓存命中不绕过版本检查；
- 教学检查、每个视口/状态的布局检查、缓存读取和版本失效分别记录 span，并报告 P50/P80/P95、最大值和失败率；端到端生成预算由 TS-04C 与 TS-06 判定；
- 已验证缓存场景 P80 目标 8 秒可用。

## TS-04C 真实生成视觉表现与讲解一致性

### 问题

冻结版本的真实模型能否把通过 TS-03 的完整讲解计划稳定转换为儿童可理解、符合主设计语言、与知识结论和旁白文本一致，并通过 TS-04A 安全门禁与 TS-04B 教学/布局门禁的互动视觉场景？

本切片验证的是带完整讲解上下文的互动视觉场景，不是孤立画面或导出的 MP4。真实配音音色和音画同步由 TS-07 验证，但每个样本必须带完整旁白文本与时间线 cue 才能评审。

### 样本与呈现方式

- 校准轮：小学声音与初中数学各 5 个样本，不计入正式分母；冻结生成提示、模型参数、评分 rubric 和门槛后才能进入正式轮；
- 正式轮：两个固定学段各 20 个样本，共 40 个；不得用只挑成功样本的方式更换分母；
- 每个样本在本地审阅库中同时呈现 `StageProfile`、讲解目标、来源与核心结论、完整旁白/字幕文本、时间线 cue、真实沙箱场景、静态降级、迁移题和机器门禁结果；
- 动画以可播放、暂停、拖动和执行规定互动的实时场景呈现，不以录屏代替运行证据；
- 固定 `390 × 844` 手机画布与基准平板画布分别审阅，可并排比较或明确切换；失败样本仍保留在仅限评审的隔离区，不进入儿童展示或有效缓存；
- 评审记录区分机器安全/布局结论、学科人工结论和产品视觉结论，不允许模型自评替代人工结论。

### 自动验证

- Schema、渲染成功、空白画布、控制台错误、资源请求、沙箱结果和销毁残留；
- 手机与平板的起始、关键过程、最终、暂停、恢复、交互后和减少动态效果状态；
- 画布安全内边界、非矩形内部安全区、完整运动包络、文字/公式溢出和隐藏裁切；
- 旁白文本、时间线 cue、视觉状态和互动结果的结构化一致性；
- 首次生成、唯一一次修复、最终通过、静态降级、缓存资格和全部阶段耗时；
- 每次生成保存模型、冻结参数、输入摘要、代码哈希、知识/讲解/布局契约版本、Token 和成本。

### 人工验证

- 对应学科审核人确认事实、公式、因果链、解题步骤、迁移题和学段适配；
- 针对 TS-03 人工审核提出的“讲解偏短”风险，逐案检查问题所需的结论、原因/机制、可观察证据、条件边界和迁移是否完整覆盖；不要求每题机械包含全部五项，但缺少实现学习目标所需的关键环节时不得评为可展示；
- 产品视觉审核人结合旁白文本与动画确认观察焦点、信息密度、变化可辨识性、互动意图、主设计风格一致性和儿童展示资格；
- 技术门禁、学科内容和产品视觉三项必须分别通过，不能用总平均分抵消任一硬失败；
- 儿童是否真正看懂、是否优于静态替代方案仍属于后续有授权的儿童可用性研究，不由本切片宣称。

### 候选门槛

- 严重知识错误展示给儿童为 0；未通过 TS-04A/TS-04B 的场景展示或进入有效缓存为 0；
- 首次生成通过 TS-04A/TS-04B 机器门禁的比例不低于 70%，只允许一次受控修复，修复后机器门禁通过率不低于 90%；
- 不能通过的样本使用合规静态降级覆盖率 100%；
- 产品视觉人工评为“可展示”的正式样本不低于 80%，学科严重错误为 0；
- 推理链覆盖不足的样本不得通过“可展示”评审，不能用动画丰富度、旁白字数或平均分抵消；
- 全新生成加全部检查 P80 不高于 30 秒，约 45 秒仍未通过时 100% 进入硬降级；
- 每一阶段分别记录 P50/P80/P95、最大值、超时率、重试、Token 与成本，不以平均值替代长尾；
- 两个知识簇必须分别报告，结果不得外推为小学至初中全年级或任意知识均可稳定生成。

### 当前结果（2026-07-30）

TS-04C 使用 DeepSeek 官方 API `deepseek-v4-pro`、`thinking.type=disabled`、`temperature=0` 和合成密封输入完成 10 例校准及每例唯一一次受控修复。紧凑架构只让模型生成一份响应式代码和元数据，本地程序在手机/平板各 8 状态运行并生成 TS-04B 声明。修复轮 10/10 响应且紧凑契约 10/10，P80 为 31,151ms，共记录 68,394 Token；实际账单成本未独立取得。

最终 10 个修复候选共执行 160 次浏览器状态检查，单状态 P80 为 35ms；18 次仍为生成代码错误，其余候选仍有字体/图形过小、事实/旁白不一致和声明结构异常。完整机器门禁仅 1/10 通过，低于修复后 >=90% 门槛，结论为 `fail`。失败场景未展示或进入有效缓存，正式 40 例和人工学科/产品展示审核未开始。当前 PRD 与高保真流程不变；架构上保留“模型紧凑输出 + 本地确定性声明/沙箱门禁”，但该模型配置不得进入产品实现。若继续验证，需另立候选方案（如受限视觉 DSL/受信任绘图组件或经重新授权的模型/服务），不得追加第二次修复覆盖本轮失败。数据边界仍仅包含合成密封知识，不含受控教材正文或儿童数据。

后续独立候选 TS-04C-v2 将模型输出收窄为无代码的 `visual-dsl/0.1`，由可信本地编译器负责响应式布局、8 状态、沙箱绘制与声明。DeepSeek 官方 `deepseek-v4-pro` 在关闭思考、`temperature=0` 的 10 例校准中首轮 10/10 通过 DSL 合同，无需修复；模型 P80 为 3,076ms，共 12,496 Token。加强门禁后，10/10 候选、手机/平板共 160/160 状态通过，内容缺失 0 次，单状态 P80 为 34ms。但 2026-07-31 产品负责人逐例审阅后判定 10/10 动画均不可展示，且 0/10 具备完整多拍旁白。四类模板只能稳定绘制矩形、标签和波形，不能表达陌生问题所需的过程、关系、公式变形和新构图；产品视觉硬门槛不能由机器通过率抵消。因此 TS-04C-v2 结论为 `fail`，独立学科评审与正式 40 例按停止条件不再启动。

TS-04C 的核心问题据此调整为陌生问题的开放场景生成质量，而不是继续复刻声音和完全平方公式 Demo。TS-04C-v3 将 Demo 仅作为 `reference_fixture` 质量参照，使用 10 道与 Demo 不同的 synthetic `gold_fixture` / `adversarial_fixture` 校准通用矢量场景图与 3–6 拍旁白时间线。模型只跨一个声明式场景接口；可信本地渲染器隐藏手机/平板重排、8 状态、边界检查、播放执行和静态降级。该候选最初只推进到 `harness_ready`；在供应商、模型/服务固定版本、调用方式、参数、预算和数据边界重新确认前未发起候选调用。

2026-07-31，用户确认以 DeepSeek 官方 `deepseek-v4-pro`、关闭 thinking、`temperature=0`、每次最多 5,000 输出 Token、60 秒超时、10 次请求、零重试/修复和总 Token 100,000 上限执行 TS-04C-v3，只发送 synthetic unverified 夹具。10/10 请求收到响应，总用量 48,835 Token，模型耗时 P80 为 23,367ms；但候选合同仅 6/10 通过。四个失败包括未声明节点引用、单动作目标超限、Schema 不支持 `morph` 宽高变化，以及动作时间越过 beat，说明模型遵循性和 `open-visual-scene/0.1` 接口均未成熟。该结果低于进入浏览器所需的 10/10 门槛，TS-04C-v3 结论为 `fail`；浏览器 8 状态、人工视觉/学科评审和正式轮均未启动，陌生问题视觉讲解质量仍未验证。

随后 `open-visual-scene/0.2` 只修复动作目标上限和 `morph` 宽高变化两个已证实的接口缺口，首轮冻结响应的诊断重放从 6/10 提升到 8/10；重放只改声明版本，不计为候选证据。用户再次确认同一 DeepSeek 配置、独立 10 请求和 100,000 Token 上限后，新轮 10/10 收到响应，总用量 49,709 Token，P80 23,708ms，但合同通过率降为 5/10。四例改用 Schema 未允许的 `children` 或非字符串 `parent_id`，一例改写唯一 claim ID。两轮累计 20 请求、98,544 Token，未取得账单成本；v0.2 冻结，不再通过放宽事实或结构合同追逐输出变体。下一候选应重新授权支持严格 JSON Schema 结构化输出的模型或服务，以相同 10 题和门槛做对照；陌生问题动画质量仍未进入浏览器与人工验证阶段。

用户随后将候选切换为 DeepSeek 官方 `deepseek-v4-flash`，并确认其余 v0.2、10 题、固定参数、单轮预算和数据边界不变。Flash 轮 10/10 收到响应，7/10 通过合同，总用量 49,489 Token，P80 13,941ms、最大 17,068ms；相同 v0.2 下 V4 Pro 为 5/10、P80 23,708ms。Flash 更快且合同通过率提高 20 个百分点，但三例仍因未允许的直线端点字段、优先级超限或非字符串 `parent_id` 失败，低于 10/10 门槛。三轮累计 30 请求、148,033 Token，实际账单成本未独立取得；Flash 同样未进入浏览器 8 状态与人工动画评审。

用户随后只授权对 Flash 首轮 3 个失败样例各修复一次，最多 3 请求、30,000 Token，模型、v0.2、固定参数和数据边界不变。修复 3/3 收到响应且 3/3 通过合同，使用 19,103 Token，P80 13,324ms；合并后的固定 10 题达到合同 10/10，但首轮结果仍单独保留为 7/10。真实 Chromium 随后对合并候选完成手机/平板各 8 状态共 160 次运行，浏览器候选仅 7/10 通过。影子、移动直线交点和勾股拆拼三题同时出现逻辑与屏幕边界违规，人工学科和产品视觉评审未启动。

复核确认 `open-visual-scene/0.2` 只给出坐标范围，没有定义 `x/y` 是元素中心还是左上角；渲染器按中心点解释，而失败候选包含另一种理解。不得逐元素启发式猜测，因此该轮归类为接口失败，不能据此判定模型动画质量。下一 harness 接口 `open-visual-scene/0.3` 固定中心点坐标，并在调用、展示或缓存前检查初始几何及变换动作的保守运动包络；缺失 anchor、左上角式歧义和运动越界均已有负向回归。旧 v0.2 输出不自动升级为 v0.3 候选证据；再次调用模型前需重新确认模型版本、参数、请求和 Token 预算与数据边界。

用户随后确认 DeepSeek 官方 `deepseek-v4-flash` 以 v0.3、相同 10 题、`temperature=0`、关闭 thinking、每次最多 5,000 输出 Token、60 秒超时、零重试/修复、10 请求和 100,000 Token 上限执行。10/10 请求收到响应，总用量 54,641 Token，P80 15,382ms、最大 23,645ms，但合同仅 5/10 通过。三个失败继续使用未允许的直线端点别名，两题使用超范围布局优先级，一题大量使用不符合小写 ID 规则的标识符；没有失败来自中心点、初始几何或运动包络。v0.3 解决了已知坐标歧义，但当前 Flash 配置的严格结构遵循仍不足，浏览器与人工评审按门槛未启动，陌生问题动画质量仍未验证。后续不得以放宽 Schema 追逐输出变体；修复失败样例或切换严格结构化输出候选均需重新授权。

产品负责人随后要求所有严格输出格式都提供实际实例。新增的 `v03-complete-format-example/0.1` 提示配置包含一份无省略、带实际字段值并通过同一 v0.3 门禁的完整 JSON，覆盖 8 种节点、10 种动作、中心点/点列几何、ID、父子引用、优先级、时间线、事实绑定、交互和静态降级；同时明确展示合法 `points` 及禁用 `x1/y1/x2/y2`、`children`、`null parent_id`、非法 ID 与超范围优先级。实例标记为 `gold_fixture` / `synthetic_format_only`，不得计为候选能力证据或复用为题目事实。离线测试 14/14 通过，当前仅为 `harness_ready`，没有发起新的外部请求；再次校准前仍需独立冻结并授权候选对象、参数、预算和数据边界。

用户随后确认沿用相同 DeepSeek 官方 `deepseek-v4-flash`、v0.3、固定参数、10 请求、100,000 Token 上限和 synthetic unverified 数据边界，仅加入完整格式实例。10/10 请求收到响应且合同 10/10，通过上一轮五类格式失败；总用量 72,796 Token，P80 15,327ms、最大 18,668ms，零自动重试与零修复。真实 Chromium 对手机/平板各 8 状态共运行 160 次，10/10 候选通过，无逻辑/屏幕边界或空白画布违规，唯一控制台错误为无关的 `favicon.ico` 404。候选状态推进为 `candidate_run_complete`，人工产品视觉与学科评审仍为 `not_started`；首次视觉抽查已见标签压图与构图稀疏风险，机器通过不得表述为动画质量通过。

产品负责人随后完成固定 10 题人工评审：1 题可展示、7 题需要修改、2 题不可展示。直线与勾股题图形语义错误，其余主要存在对象不可辨认、公式/构成关系缺失、旁白动作未同步和无用途图形；v0.3 因而判定 `fail`。评审还发现渲染器未真实绘制旋转、`trace` 会整条闪现且播放器未响应减少动态效果；修复后真实 Chromium 的 6 组像素回归及旧候选手机/平板 160 状态全部通过，但该运行时修复不改变旧候选的人工失败结论。

质量修订接口 `open-visual-scene/0.4` 只增加通用多边形、箭头、形状内标签和 required-aspect 到节点/讲解拍的可检查绑定，没有增加题型组件或任意代码。用户确认同一 DeepSeek Flash、固定参数、10 请求、100,000 Token 和 synthetic unverified 边界重新校准；10/10 收到响应，使用 84,818 Token，P80 14,105ms，但合同仅 5/10。影子与杠杆题暴露动态 point 端点接口缺口；分数、凝结和直线题分别出现不存在的 beat 引用或坐标越界。低于 10/10 门槛，浏览器候选门禁和人工复评未启动，TS-04C 最终仍为 `fail`。PRD、高保真、数据和隐私边界不变；下一候选前需先决定是否为关系线定义端点绑定/重算语义，并重新授权调用预算。

最后以 DeepSeek 官方 `deepseek-v4-flash` 对一道 synthetic unverified 鸡蛋盐水题执行问题级完整讲解校准：一次请求在 11,587ms 内生成 4 段、32 秒讲解计划，使用 4,709 Token；合同通过，手机/平板 56/56 个布局与时间轴状态通过。但第三段用等长箭头表达“浮力 > 重力”，第四段说明最终平衡却没有生成平衡受力箭头，因此教学语义门禁失败。该候选未修复、未展示或缓存。产品负责人认为现有视觉可行性证据已足够，不再追加题目或模型调用；TS-04C 以 `fail` 收尾，不外推为陌生题泛化或学习效果证据。

## TS-05 追问、进度拖动与现场恢复

### 最小路径

1. 子片段播放并完成轻拨/用力拨；
2. 拖动进度到儿童操作前，场景同步恢复且不自动重演操作；
3. 手机确认追问后浮层自动收起；
4. “慢一点”修改原场景；
5. “如果没有空气”创建关联主题；
6. 返回并恢复主题、子片段、时间、力度和慢放；
7. 注入重复、乱序和离线事件。

### 候选门槛

- 本地暂停反馈小于 150ms；
- 黄金路径状态恢复一致率 100%；
- 拖动后音频、文字、图片、标注和场景一致；
- 不自动重演儿童操作；
- 重试不创建重复主题或事件。
- 暂停反馈、快照保存、原场景修改、关联主题创建、主题返回和完整恢复分别记录 span；P95 与最大值必须按本地/联网、首次/重试分开报告。

## TS-06 渐进生成、延迟和硬降级

TS-06 不重新定义局部计时器，只汇总同一 `StageTiming` trace 中由 TS-02、TS-03、TS-04A、TS-04B、TS-04C 和 TS-07 产生的 span，并验证并行准备、重试、超时和降级后的真实墙钟时间。

核心指标为 `question_confirmed → first_meaningful_content`、`question_confirmed → interactive_ready` 和失败路径的 `question_confirmed → fallback_ready`。候选目标：首个有意义讲解 P80 小于 8 秒；全新场景 P80 小于 30 秒；约 45 秒仍未通过时 100% 硬降级。确认、加载提示和问题复述不计入首个有意义讲解；孩子确认前的 OCR/ASR 系统耗时由 TS-01 单独报告，孩子阅读和决定时间排除。

TS-06 按冷/热启动、缓存命中/未命中、首次/修复、小学/初中、手机/平板、成功/失败/降级分别报告样本数、P50/P80/P95、最大值、超时率和总成本，并验证重试不会重复扣费或重复写入事件。

## TS-07 统一播放时间轴

沿用 v1，并增加拖动后的全现场重建、儿童操作不自动重演、手机/平板布局切换和预生成音频文件。分别记录媒体准备、首次播放、暂停反馈、恢复、拖动重建、片段切换和前后台恢复 span，以及语音/字幕/视觉 cue 偏差与累计漂移；按真实音频和视觉降级两条路径报告 P50/P80/P95 和最大值。正常 cue 偏差及暂停恢复误差 P95 小于 250ms。

## TS-08 事件、分支、版本和修订

增加内容版本批量失效：人工确认错误后，系统停用版本、定位受影响记录、使学习结果失效并生成重新验证任务。历史保留原版本事实，继续探索使用新版本。事件追加、离线提交、完整回放、分支创建、批量失效和重新验证任务创建分别记录 span；按事件量与受影响记录量报告 P50/P80/P95、最大值和失败率。

## TS-09 固定安全响应与熔断

### 最小实现

- 危险实验、医疗、隐私、不适龄、自伤/他伤/受虐和紧急危险夹具；
- 绕过临时知识包、动态场景和学习判定；
- 最小风险类别记录，不保存完整敏感原话；
- 具体场景、生成路径和安全错误熔断；
- 不暗示真人介入或自动通知家长。
- 记录本地风险分类、固定响应选择、响应可见和熔断生效 span；安全正确性是硬门槛，延迟按风险类别报告 P50/P80/P95 与最大值，不用更快响应抵消误放行。

公开内测前，固定模板和通知边界必须另行接受儿童安全与法律专业评审。

## TS-10 数据生命周期与授权

- 同页提供“保留 30 天”和“处理后删除”两个主动选择，不预选；
- 原始媒体隔离、到期自动删除、撤回和账号删除；
- 探索记录默认 90 天，可选 30 天或不保存；
- 级联删除及备份清理；
- 审计不保留被删儿童内容。
- 分别记录撤回确认、主存储删除、派生记录级联、备份清理和最终不可读取验证时间；同步用户反馈与后台最终清理分开报告 P50/P80/P95、最大值和失败率。

## TS-11 学习结果与家长分类

- 结论与因果验证分别保存；
- 结果为自己弄懂了、部分理解、建议陪一陪或未完成；
- 完成方式为首次、系统提示后或家长帮助后；
- 家庭协助缺失保持未确认；
- AI 核验和临时知识包结果为探索性证据；
- 未核验生成只记录探索与反馈，不生成正式学习结果；
- 不生成近期学习画像。
- 结果计算、证据定位、删除后重算和家长详情读取分别记录 span；按证据量报告 P50/P80/P95、最大值和失败率，分类正确性不得由性能指标抵消。

## TS-12 可观测性、成本与故障恢复

最小 `StageTiming` v1.0、trace 关联、Token/成本、缓存、重试和降级字段从 P0 起即为所有切片的公共前置能力；TS-12 完整切片仍在公开儿童内测前验证 1/10/50 并发、供应方超时、日志脱敏、指标完整性和故障恢复。增加知识来源版本、AI 核验步骤、代码哈希、沙箱策略、正确性测试、缓存命中、熔断和修订批次；日志不包含原始媒体或完整敏感原话。TS-12 必须证明能从一次端到端 trace 定位具体失败阶段并计算完整探索与追问成本。

## 4. 执行顺序

### P0：决定核心方案是否可行

```text
TS-00 → TS-02 → TS-04A → TS-04B → TS-03 → TS-04C → TS-07 → TS-05 → TS-06
```

TS-02 与 TS-04A 可在资源允许时并行准备，但正式依赖关系按上图判断。TS-04C 同时依赖 TS-03 的完整讲解计划、TS-04A 的安全执行边界和 TS-04B 的教学/布局门禁；TS-05 依赖 TS-07 的统一真实时间轴。`StageTiming` 最小公共能力从所有 P0 切片开始启用，TS-12 后续验证完整负载与故障恢复。

### 公开儿童内测前

完成 TS-01、TS-08、TS-09、TS-10、TS-11 和 TS-12 的完整验证，并关闭所有严重错误。

## 5. 每个切片的完成定义

每个切片必须包含：唯一问题、非目标、固定夹具、Schema、运行命令、环境、指标、门槛、失败样例、成本、数据处理、结论和影响，并使用 `StageTiming` 记录其负责的局部 span。涉及内容、讲解或视觉产物的切片还必须记录 `StageProfile`、分学段结果和跨学段失败样例。

结论必须写明：

- 对 PRD 的影响；
- 对高保真和公开 Demo 的影响；
- 对架构的影响；
- 对数据和隐私的影响；
- 已验证范围；
- 仍未验证范围；
- 通过、有条件通过或不通过。

双学段矩阵通过时，结论最多表述为“固定小学科学与初中数学夹具可通过同一知识、讲解和视觉契约运行”。不得表述为“小学至初中全年级已覆盖”“学段适配已经产生学习效果”或“任意知识均可稳定生成”。

演示成功一次、模型自评、平均延迟或无失败截图都不能替代可复核结果。
